services:
  - type: web
    name: focus-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    disks:
      - name: data
        mountPath: /app/app/database
        sizeGB: 1
